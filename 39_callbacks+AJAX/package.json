{
  "name": "callbacks-ajax",
  "version": "1.0.0",
  "description": "une fonction callback donnée en argument d'une fonction pour récupérer les résultats d'un appel HTTP GET",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js"
  },
  "author": "guillaume@owlab",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
